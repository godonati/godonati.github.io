# Gabriele O. Donati — portfolio

Small Node.js server for the portfolio and Via Production case study.

## Run locally

```bash
npm start
```

Then open `http://localhost:3000`.

The server uses only Node's standard library, so there are no production dependencies or build step. Static assets are cached for one hour, while HTML is always revalidated.
